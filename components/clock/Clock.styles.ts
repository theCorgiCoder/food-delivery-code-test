import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    justifyContent: "flex-start",
  },
  textBox: {
    paddingLeft: 5,
    flexDirection: "row",
    textAlign: "center",
  },
});
