import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 10,
    backgroundColor: "red",
    borderRadius: 5,
    marginBottom: 10,
  },
  text: {
    color: "white",
    textAlign: "center",
    fontFamily: "Helvetica",
  },
});
